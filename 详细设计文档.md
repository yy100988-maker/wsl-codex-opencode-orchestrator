# WSL Codex OpenCode Orchestrator 详细设计文档

> 文档版本：0.1
> 编写日期：2026-08-15
> 状态：已评审，待实现

## 1. 目标

在 Windows 主机上，以 Codex Desktop 作为主控 agent，在指定的 WSL2 发行版内运行 OpenCode 子 agent，实现可恢复的多任务并行开发、分段验收和资源回收。

本技能是现有 Windows 版 codex-opencode-orchestrator 的独立 WSL 版本，不覆盖、不修改 Windows 版。

## 2. 范围

本期范围：

1. 指定 WSL2 发行版的环境预检与启动。
2. WSL Linux 文件系统优先的项目和 worktree 管理。
3. Bash 版任务调度、状态持久化、Lease、Gate 和清理闭环。
4. 同一 WSL 实例内多个独立 OpenCode 进程组并行运行。
5. WSL 内存与 Windows 主机内存双层准入控制。
6. 默认模型和备用模型的预检与回退。
7. 任务分支提交、Gate 通过后的主控合并和安全 worktree 回收。

非本期范围：

1. 不为每个 agent 启动独立 WSL 虚拟机或发行版。
2. 不混用 Windows 原生 opencode.exe 和 WSL 内的 opencode。
3. 不开发 Web 管理后台、跨机器分布式调度或测试 agent 写业务代码。

## 3. 固定职责

| 角色 | 固定职责 | 禁止事项 |
|---|---|---|
| Codex 主控 | 规划、拆分、最小 prompt、监督、Gate 验收、合并、清理编排 | 将编排职责下放给 OpenCode |
| WSL Supervisor | 读取状态、准入、启动 agent、收集 handoff、触发 Gate、回收进程 | 修改业务代码 |
| OpenCode 实现 agent | 只实现一个子任务，写 handoff，提交任务分支 | 拆分任务、改动越界文件、合并其他任务 |
| 测试 agent | 运行验收 | 编辑或写入业务代码 |

## 4. 总体架构

~~~mermaid
flowchart TD
    A["Codex Desktop 主控，Windows"] --> B["Windows 启动器"]
    B --> C["wsl.exe -d Ubuntu"]
    C --> D["WSL Supervisor，bash"]
    D --> E["WSL 内存准入"]
    D --> F["Windows 主机内存准入"]
    E --> G["任务调度"]
    F --> G
    G --> H["OpenCode 实现 agent 进程组"]
    H --> I["任务 handoff 与提交"]
    I --> J["只读测试 agent 与 Gate"]
    J -->|通过| K["主控合并任务分支"]
    J -->|失败| L["修复任务或阻塞记录"]
    K --> M["进程组和 worktree 清理"]
~~~

## 5. WSL 实例与 Agent 模型

### 5.1 单一 WSL 实例

一个项目批次使用一个指定的 WSL2 发行版，例如 Ubuntu。所有 OpenCode 子 agent 都在该发行版内运行，共享 WSL VM 的内存和 CPU 上限，以及 WSL 内的 Git、Node、Bun 和 OpenCode 工具链。

### 5.2 一个任务对应一个进程组

每个可执行任务对应一个独立的 OpenCode 进程组：

~~~bash
setsid opencode run ...
~~~

每个任务记录 task_id、pid、pgid、process_start_ticks、worktree 和 model。process_start_ticks 用于降低 PID 被系统复用时误杀其他进程的风险。

## 6. 路径和项目根目录

1. WSL 执行项目根目录必须是 Linux 路径。
2. 默认根目录为 /home/<wsl-user>/projects/<project>。
3. .opencode/worktrees 必须位于项目根目录内。
4. Windows 路径只能作为输入兼容路径，并由启动器转换为 WSL 路径。
5. worktree、任务 ID、日志目录必须使用 ASCII。
6. 项目初始位于 D:\CODEX 时，预检必须要求在 WSL Linux 文件系统创建或同步可执行副本；不得把高并发 worktree 默认放在 /mnt/d。

路径配置示例：

~~~json
{
  "windows_project_root": "D:\\CODEX\\project",
  "wsl_project_root": "/home/y2ksky100/projects/project",
  "distro_name": "Ubuntu"
}
~~~

## 7. 配置模型

项目状态目录新增 .opencode/wsl-config.json：

~~~json
{
  "schema_version": 1,
  "wsl": {
    "distro_name": "Ubuntu",
    "project_root": "/home/y2ksky100/projects/app",
    "windows_project_root": "D:\\CODEX\\app"
  },
  "models": {
    "implementation": "opencode-go/deepseek-v4-flash",
    "fallback": "opencode-go/gpt-5.6-luna",
    "validation": "opencode-go/deepseek-v4-flash"
  },
  "scheduler": {
    "max_concurrent_agents": 100,
    "max_new_agents_per_cycle": 20,
    "monitor_interval_seconds": 60,
    "task_start_delay_ms": 500
  },
  "memory": {
    "wsl_resume_below_percent": 80,
    "wsl_stop_admission_percent": 88,
    "wsl_critical_percent": 90,
    "windows_resume_below_percent": 80,
    "windows_stop_admission_percent": 88,
    "windows_critical_percent": 90,
    "windows_reserve_mb": 4096
  },
  "cleanup": {
    "term_grace_seconds": 15,
    "kill_grace_seconds": 5,
    "delete_validated_worktrees": true,
    "archive_failed_worktrees": true
  }
}
~~~

## 8. 环境预检

启动前必须通过以下检查：

1. Windows 侧 wsl.exe -l -v 能找到配置发行版，且版本为 2。
2. WSL 内存在 bash、git、jq、setsid、ps、pgrep、kill 和 opencode。
3. opencode auth 已在 WSL 用户环境完成，不读取或复制 Windows 用户凭据。
4. WSL 项目根目录存在且是 Git 仓库。
5. 项目根目录不位于 /mnt/c 或 /mnt/d，除非用户显式确认兼容模式。
6. 主模型和备用模型均可被 OpenCode 模型列表识别。
7. Windows 主机和 WSL 内存状态均满足启动最低条件。

预检失败必须输出机器可解析 JSON，并标记 manual-preparation-required，禁止启动任务。

## 9. 双层内存保护

### 9.1 WSL 内存保护

WSL Supervisor 读取 WSL 内存和 swap 指标，并根据 estimated_mb、任务历史峰值和当前活动任务计算准入。

~~~text
低于恢复阈值：允许恢复准入
达到停止阈值：停止新增任务
达到临界阈值：优先完成验收和清理
持续使用 swap：暂停新增并降低后续并发
~~~

### 9.2 Windows 主机内存保护

Windows 侧增加 host-memory-guard.ps1，每次准入前返回：

~~~json
{
  "host_used_percent": 72.4,
  "available_mb": 18432,
  "reserve_mb": 4096,
  "admission_allowed": true,
  "reason": null,
  "measured_at": "2026-08-15T12:00:00Z"
}
~~~

WSL Supervisor 通过 Windows 互操作调用该脚本并解析 JSON。任意一侧不允许准入时，不启动新的 OpenCode agent：

~~~text
admission_allowed = wsl_allowed AND windows_host_allowed
~~~

每轮状态必须记录 WSL 内存使用率、swap 使用量、Windows 主机内存使用率、可用内存、暂停原因、当前 agent 数和预计任务内存。

## 10. 任务与并发

任务结构沿用 tasks.json，新增 WSL worktree 和进程组字段。并发准入必须同时满足：

1. 依赖完成。
2. allowed_files 与运行任务不重叠。
3. WSL 内存允许。
4. Windows 主机内存允许。
5. 未达到并发上限。
6. worktree、prompt 和 Lease 完整。
7. 未超过任务最大重试次数。

## 11. 模型预检与回退

默认实现模型是 opencode-go/deepseek-v4-flash，备用模型是 opencode-go/gpt-5.6-luna。

执行规则：

1. Supervisor 启动前检查两个模型可用性。
2. 启动任务时优先使用主模型。
3. 主模型发生模型不存在、provider 不可用或启动即失败时，只回退一次到备用模型。
4. 回退事件写入 task-graph.jsonl。
5. 备用模型仍失败时，任务写为 blocked 或进入有限环境重试。

## 12. 进程组清理

每个任务以独立进程组运行。清理只针对任务 PID、PGID、启动时间和 worktree 路径联合确认的进程。

清理流程：

1. 停止该任务的新命令。
2. 校验 PID 与 process_start_ticks。
3. 向 PGID 发送 SIGTERM。
4. 等待 term_grace_seconds。
5. 仍存活时发送 SIGKILL。
6. 扫描 worktree 命令行关联的受限进程。
7. 写入清理事件。
8. 只有进程清理成功后，才能删除或归档 worktree。

禁止执行宽泛清理：

~~~bash
pkill node
pkill bun
pkill esbuild
~~~

## 13. 任务提交、Gate 和合并

### 13.1 实现任务

每个实现 agent 必须只修改 allowed_files，运行任务级测试，写入 .opencode-handoff.json，并提交到自己的任务分支。

### 13.2 测试与 Gate

测试 agent 使用只读工具权限，只能读取、搜索和运行测试命令，禁止编辑、写文件或提交。

Gate 至少包含 Scope、Handoff、Static、Unit、Contract、Integration 和 Security。

### 13.3 合并

Gate 通过后，由主控执行：

~~~text
任务分支 -> 集成分支 -> 项目目标分支
~~~

worktree 删除前必须满足：任务提交存在、提交已合并或 patch 已归档、关联进程已停止。

## 14. 状态、Lease 和恢复

状态目录位于：

~~~text
<wsl-project-root>/.opencode/
~~~

关键文件：

~~~text
tasks.json
orchestrator-state.json
task-graph.jsonl
leases.json
gates/<task-id>/result.json
controller-memory/project-state.md
reports/资源清理报告.md
~~~

任务退出后，Supervisor 根据 handoff、Lease、进程组和 Gate 结果恢复状态。handoff 的 completed 不等同主任务完成，必须通过 Gate。

## 15. 脚本规划

实施阶段创建：

~~~text
scripts/
  preflight.sh
  supervisor-loop.sh
  admission-cycle.sh
  process-manager.sh
  worktree-manager.sh
  lease-manager.sh
  gate-runner.sh
  host-memory-guard.ps1
  report-builder.sh
~~~

Windows 侧只保留启动、主机内存采样和必要路径转换；任务调度、OpenCode 调用、Git worktree、测试和 Linux 进程清理均运行在 WSL 内。

## 16. 验收标准

1. 可显式指定 WSL2 Ubuntu 发行版并通过预检。
2. opencode 在 WSL 内运行，不启动 Windows 原生 opencode.exe。
3. 多个任务在同一 WSL Ubuntu 中以不同 PGID 并行执行。
4. 任意并行任务不得修改重叠文件。
5. WSL 或 Windows 主机任一内存保护拒绝时，不启动新 agent。
6. 主模型不可用时，只回退一次到 opencode-go/gpt-5.6-luna。
7. 已完成任务的进程组和 worktree 能安全清理。
8. 测试 agent 无法修改业务代码。
9. Supervisor 重启后可依据状态和事件日志恢复。
10. Windows 版技能不受影响。

## 17. 文件链接

- [技能说明](C:/Users/y2ksk/.codex/skills/wsl-codex-opencode-orchestrator/SKILL.md)
- [[SKILL]]
- [WSL版详细设计文档](C:/Users/y2ksk/.codex/skills/wsl-codex-opencode-orchestrator/详细设计文档.md)
- [[详细设计文档]]

