# WSL Codex OpenCode Orchestrator 使用说明

## 1. 技能用途

本技能由 Codex 作为主控 agent，在指定的 WSL2 发行版中调度多个 Linux 版 OpenCode 子 agent，按代码文件边界并行开发、分段验收、合并任务分支，并清理任务进程组、worktree 和 Lease。

用户只需要提供：

1. 需求/设计文档目录。
2. 项目产出目标目录。
3. 指定的 WSL2 发行版名称，例如 `Ubuntu`。

## 2. 固定职责

- Codex 主控：负责需求读取、任务拆解、任务图、prompt、监督、Gate、合并和清理。
- WSL Supervisor：负责预检、准入、调度、状态恢复、Gate 调用和资源回收。
- OpenCode 子 agent：只实现一个子任务，不负责任务编排和跨任务协调。
- 测试 agent：只读执行测试和验收，不修改业务代码。

## 3. 标准启动提示词

复制下面的提示词到 Codex：

```text
使用 $wsl-codex-opencode-orchestrator 启动项目开发。

需求/设计文档目录：
D:\项目\docs

项目产出目标目录：
D:\项目\workspace

指定 WSL2 发行版：
Ubuntu

请严格执行以下流程：

1. 由 Codex 作为主控 agent，负责需求读取、项目分析、任务拆解、任务图规划、进度监督、分段验收、合并和最终清理。
2. 读取需求文档、详细设计文档、接口文档、数据库设计、测试说明和部署说明。
3. 只完整读取设计文档一次，建立项目知识库和主控项目记忆。
4. 自动生成：
   - .opencode/wsl-config.json
   - .opencode/tasks.json
   - .opencode/prompts/<task-id>.md
   - .opencode/knowledge-base/
   - .opencode/controller-memory/
5. 按代码文件不并行占用原则拆分任务：
   - 每个子任务只负责一个明确模块或文件集合；
   - allowed_files 和 claimed_files 不得与活跃任务重叠；
   - 共享类型、Schema、配置、迁移和入口文件优先串行；
   - 独立模块尽量细拆，任务数量可以达到 100+；
   - 实际并发不超过 100，并受 WSL 和 Windows 主机内存限制。
   - 生成任务后执行 `bash scripts/task-decompose.sh <project-root>`，将目录任务展开成实际源码文件任务，并为重叠文件建立串行依赖。
6. 每个 OpenCode 子 agent 只接收当前模块的设计切片、必要契约、依赖结果、允许文件、测试命令和验收条件。
7. OpenCode 只负责单个子任务实现，不得自行拆解任务、修改越界文件、协调其他 agent 或合并代码。
8. 测试 agent 只负责只读测试、Gate 验收和结果判定，不得修改业务代码。
9. 在指定 WSL Ubuntu 内运行 Linux 版 OpenCode，不使用 Windows 原生 opencode.exe。
10. 启动任务前自动检查并安装缺少的 Ubuntu 系统依赖；发现 Windows wrapper 时自动删除当前用户的 `~/bin/opencode` wrapper，安装 Linux 版 OpenCode 到 `~/.opencode/bin`，并写入 `~/.profile`；不得自动修改 OpenCode auth、API key 或模型配置。
11. 默认使用 opencode-go/deepseek-v4-flash；不可用时只回退一次到 opencode-go/gpt-5.6-luna。
12. 每 30 秒检查 WSL 内存、Windows 主机内存、运行 agent、完成任务、失联任务、文件 Lease、进程和 worktree 状态，并将 `scheduler.monitor_interval_seconds` 设置为 `30`。
13. 内存低于 80% 时恢复准入；达到 88% 时停止新增任务；达到 90% 时优先验收和清理。
14. 每个任务使用独立 branch、worktree、Lease、prompt、日志和 OpenCode 进程组。
15. 子任务完成后必须检查 handoff、changed_files、任务测试和 Gate，并清理当前任务的进程组和 worktree，释放 Lease，更新状态和报告。
16. 禁止执行 pkill node、pkill bun、pkill esbuild 或其他全局进程清理命令。
17. 主控必须持续读取磁盘状态和项目记忆，不依赖长对话上下文。
18. 所有任务完成后生成开发完成、测试验收、阻塞项、资源清理和 Token 执行成本报告。

WSL Linux 项目目录是唯一事实来源。需要把状态交付到 Windows 目录时，执行 `bash scripts/delivery-sync.sh .opencode/wsl-config.json`；该命令只同步状态、报告、Gate、归档和记忆，不覆盖业务代码。

开始前先执行 WSL 环境预检，并报告 WSL 发行版、项目根目录、OpenCode 路径、模型、WSL 内存、Windows 主机内存、可启动任务数量和预计并发数量。预检通过后再开始自动开发。
```

## 4. 继续未完成任务提示词

```text
使用 $wsl-codex-opencode-orchestrator 继续执行当前项目批次。

项目产出目标目录：
D:\项目\workspace

指定 WSL2 发行版：
Ubuntu

请读取 .opencode/tasks.json、orchestrator-state.json、task-graph.jsonl、leases.json、Gate 结果、handoff、controller-memory 和资源清理报告。

恢复已有任务状态，不重复创建已完成任务，不重复发送完整设计文档。

先清理已结束或失联的 OpenCode、node、bun、npm、esbuild 派生进程，再根据 WSL 和 Windows 主机内存重新计算可启动任务。

继续执行未完成任务，并严格遵守：
- Codex 是主控；
- OpenCode 只执行单个实现任务；
- 测试 agent 只读验收；
- 主模型为 opencode-go/deepseek-v4-flash；
- 主模型不可用时只回退一次到 opencode-go/gpt-5.6-luna；
- 任务完成后必须执行 Gate、清理进程、清理 worktree、释放 Lease 并写入报告。
```

## 5. 手动启动 Supervisor

```powershell
wsl.exe -d Ubuntu -- bash -lc 'cd /home/<user>/projects/<project> && bash /path/to/wsl-codex-opencode-orchestrator/scripts/supervisor-loop.sh .opencode/wsl-config.json'
```

Supervisor 启动时会自动：

1. 获取项目单实例锁。
2. 安装缺少的 `curl`、`git`、`jq`、`util-linux`、`procps` 等系统依赖。
3. 检查并自动安装 Linux 版 OpenCode，确保 `command -v opencode` 指向 `~/.opencode/bin/opencode`，验证版本。
4. 检查 Git、项目路径、模型和认证状态。
5. 检查 WSL 与 Windows 主机双层内存。
6. 按依赖、文件 Lease 和内存预算启动任务。
7. 运行 Gate，合并通过任务，归档失败任务。
8. 清理任务进程组、worktree 和 Lease。

## 6. 前置检查

```bash
bash scripts/ensure-dependencies.sh
bash scripts/ensure-opencode.sh /home/<user>/projects/<project>/.opencode/wsl-config.json
bash scripts/runtime-preflight.sh /home/<user>/projects/<project> /home/<user>/projects/<project>/.opencode/wsl-config.json
bash scripts/task-decompose.sh /home/<user>/projects/<project>
bash scripts/preflight.sh /home/<user>/projects/<project>/.opencode/wsl-config.json
```

OpenCode 需要在目标 WSL 用户环境中完成：

```bash
opencode auth
opencode models
```

## 7. 运行产物

```text
.opencode/
├── tasks.json
├── wsl-config.json
├── orchestrator-state.json
├── leases.json
├── events/
├── gates/<task-id>/result.json
├── logs/<task-id>.log
├── processes/<task-id>.json
├── prompts/<task-id>.md
├── worktrees/<task-id>/
├── archives/<task-id>/
└── reports/资源清理报告.md
```

## 8. 重要边界

- 不为每个 agent 创建独立 WSL 虚拟机。
- 不混用 Windows 原生 OpenCode 和 WSL OpenCode。
- 不使用全局进程名清理。
- `handoff.completed` 不等于最终完成，必须通过 Gate。
- 测试 agent 不修改业务代码。
- `.opencode/` 默认是运行产物，不应直接提交到业务仓库。

## 9. 相关文档

- [README](README.md) / `[[README.md]]`
- [快速开始](docs/快速开始.md) / `[[快速开始.md]]`
- [配置参考](docs/配置参考.md) / `[[配置参考.md]]`
- [任务协议](docs/任务协议.md) / `[[任务协议.md]]`
- [运行机制](docs/运行机制.md) / `[[运行机制.md]]`
- [故障排查](docs/故障排查.md) / `[[故障排查.md]]`
- [详细设计文档](详细设计文档.md) / `[[详细设计文档.md]]`
